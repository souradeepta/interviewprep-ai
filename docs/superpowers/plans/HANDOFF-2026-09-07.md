# Handoff — ML/AI Interview Preparation Expansion

**Date:** 2026-09-07
**Current remote:** `master` at `4912036`
**Repository:** `interviewprep-ml`

## Current state

The repository now has expanded, testable ML/AI interview-preparation paths
covering ML coding, LLM engineering, agentic AI, ML system design, SQL/data
leakage, evaluation, and production reliability.

The latest work was planned by Terra and implemented by Luna. The latest
repair pass addressed correctness issues found in an independent Terra review.

## Delivered checkpoints

- `b88da10` — repaired CV/statistics notebooks and added regression coverage.
- `633b336` — reconciled enhancement-plan and handoff status.
- `f74afe1` — closed repository review gaps, repaired active links, and added
  integrity validation.
- `5e1cc64` — removed stale handoff instructions and updated delivery status.
- `3430010` — expanded current ML/AI interview coverage from Reddit, LeetCode,
  NeetCode, and Deep-ML signals.
- `682f8c8` — added LLM, agent, ML coding, SQL, evaluation, and system-design
  interview practice paths.
- `4912036` — hardened NDCG, agent reliability, SQL attribution, point-in-time
  joins, simulation-card contracts, and regression tests.

## Important files

- Planning and repair specs:
  - `docs/superpowers/specs/2026-09-07-ml-ai-interview-practice-expansion.md`
  - `docs/superpowers/specs/2026-09-07-interview-practice-correctness-repair.md`
- ML coding and solutions:
  - `ml/interview-prep/ml-coding-questions.md`
  - `ml/interview-prep/solutions/`
- Data and evaluation practice:
  - `ml/interview-prep/data-sql-questions.md`
  - `ml/interview-prep/sql/`
  - `ml/interview-prep/evaluation-experiment-questions.md`
- LLM and agent practice:
  - `llm/interview-prep/`
  - `agentic-ai/interview-prep/`
- System-design practice:
  - `system-design/interview-prep/question-bank.md`
  - `system-design/interview-prep/scorecard.md`
- Regression coverage:
  - `tests/test_interview_bank_contracts.py`
  - `tests/test_ml_interview_solutions.py`
  - `tests/test_ml_sql_exercises.py`
  - `tests/test_agent_interview_exercises.py`
  - `tests/test_interview_prep_expansion.py`

## Validation

The latest validation passed:

- `426 passed, 3 skipped`
- Python syntax checks for new and modified modules
- SQLite fixture and query checks
- Active Markdown link audit: `0 problem(s)`
- `git diff --check`

The three skipped tests are notebook runtime smoke tests; this environment has
no installed `python3` Jupyter kernel. Structural notebook checks still run.

## Preserved workspace state

Do not delete or reset prior-run material. Preserve:

- `.claude/worktrees/**`
- recovered or untracked helper scripts, including `scripts/gen_nlp_notebooks.py`
- historical plans and handoffs
- generated caches unless cleanup is explicitly requested

The original workspace Git metadata is on a read-only mount. Its working tree
may therefore appear dirty even after the remote is current. Use the writable
staging clone at `/tmp/interviewprep-ml-push` for delivery:

1. Copy only an explicit reviewed file list into the staging clone.
2. Exclude `.claude`, `__pycache__`, `.pytest_cache`, and uncertain artifacts.
3. Confirm the staging clone remote is
   `git@github.com:souradeepta/interviewprep-ai.git`.
4. Run staged `git diff --check`, commit, push, and verify with `git ls-remote`.

The Git workflow and failure analysis are recorded in the global
`/home/sbisw/github/AGENTS.md` and `MEMORY.md` files.

## Recommended next work

No blocking correctness work remains from the latest Terra review. Future
improvements should be additive and ML/AI-specific:

1. Add concrete remediation pages for the highest-value LLM and agent prompts.
2. Expand SQL drills with more expected-result assertions without introducing
   real user data or external services.
3. Add an optional CI job with a Python Jupyter kernel for notebook execution.
4. Periodically re-audit current interview signals and update this handoff with
   a new dated checkpoint rather than rewriting historical handoffs.

Before starting new work, verify the remote ref and read the two dated specs
above. Do not resume the obsolete Stats or repository-expansion batches from
older handoffs.
