# Handoff — ML/AI Interview Preparation Expansion

**Date:** 2026-09-09
**Repository:** `interviewprep-ml`
**Content checkpoint:** `master` at `4fa5e8d`

## Update: 2026-09-12

On 2026-09-12, a disaster-recovery commit `d5cf4af` ("feat: expand interview-prep 
content, tests, and CI workflow") was created and pushed to `origin/master`, 
capturing ~110 modified markdown/notebook files and 50+ new files across ML, LLM, 
and agentic-AI domains. This includes:
- New CI workflow (`.github/workflows/notebook-smoke.yml`)
- 20+ new coding algorithm guides and solutions
- 7 new test files for interview exercises, SQL, and agent exercises
- Dated handoff docs and spec documents for session continuity
- Updated `.gitignore` to exclude `.claude/` (259MB worktree state) and Python 
  artifacts (`__pycache__`, `.pyc` files)
- Comprehensive notebook and markdown expansions across all domains (172 files, 
  9930 insertions)

The `.git` directory size is 36MB (not bloated by excluded artifacts). Repository 
remains fully resumable from git alone.

## Outcome

The ML/AI interview-preparation expansion remains complete and validated. This
checkpoint strengthens the SQLite practice track with a fifth executable
reference answer: a leakage-safe rolling 24-hour event count. It also adds
exact expected-result contracts for the existing feature-aggregation and
point-in-time join answers.

A clean clone exposed one pre-existing documentation-contract failure: the
expansion specification did not link to its correctness-repair follow-up. The
missing status/link was restored as part of the same content checkpoint.

## Changed files

- `ml/interview-prep/data-sql-questions.md`
- `ml/interview-prep/sql/01-feature-aggregations.sql`
- `ml/interview-prep/sql/02-point-in-time-joins.sql`
- `ml/interview-prep/sql/05-rolling-prior-count.sql`
- `tests/test_ml_sql_exercises.py`
- `tests/test_interview_prep_expansion.py`
- `docs/superpowers/specs/2026-09-07-ml-ai-interview-practice-expansion.md`

The SQL contracts now verify duplicate-delivery removal, exclusion of missing
entities, strict point-in-time feature selection, exclusion of the current
event, and exact rolling-window output rows. Test discovery executes every
numbered SQL reference answer rather than a hard-coded `01`–`04` range.

## Checks

- Full suite: `429 passed, 3 skipped`
- Focused SQL/interview suite: `11 passed`
- Active Markdown link audit: `0 problem(s)`
- Staged diff check: clean

The three skipped checks are notebook runtime smoke tests because this
environment has no installed `python3` Jupyter kernel. Structural notebook
validation remains active.

## Delivery and preserved state

- Content commit: `4fa5e8d` (`feat: strengthen SQL interview result contracts`)
- This handoff is delivered in the following documentation-only commit.
- `.claude/**`, `__pycache__/**`, `.pytest_cache/**`, and uncertain workspace
  artifacts were not staged.
- The original worktree still uses read-only Git metadata; future delivery
  should continue through a verified writable clone.

## Follow-on work delivered after the original handoff

After the pushed `4fa5e8d` checkpoint, the SQL track was expanded with
reference answers and exact-result tests for sessionization, missing-entity
quarantine, as-of boundary leakage, NULL-label metric auditing,
temporal/entity-safe splits, and late-arrival availability. The fixture now
includes synthetic ingestion timestamps while preserving duplicate deliveries.

- Focused SQL/interview suite: `17 passed`
- Full suite: `435 passed, 3 skipped`
- Active Markdown link audit: `0 problem(s)`

The SQL follow-on was delivered in `1996a7c`.

The remediation continuation added two focused pages:

- `llm/interview-prep/retrieval-failure-remediation.md`
- `agentic-ai/interview-prep/retry-idempotency-remediation.md`

They are linked from Q11/Q2 and the section READMEs, respectively, and were
delivered in `1c75b70`. The subsequent CI continuation also adds
the opt-in [notebook smoke workflow](../../../.github/workflows/notebook-smoke.yml),
which installs a Python Jupyter kernel and executes deterministic statistics,
computer-vision, NLP, RL, and AI-fundamentals representatives without
downloading a model.

The workflow/test matrix extension was delivered in `8f0fc6a`. The verified
remote checkpoint is `master` at
`9f0b3854684e3c72764b0eb5a6c16b960e837d51`.

The latest validation is `435 passed, 6 skipped`; the six skips are the
environment-dependent notebook runtime checks in this kernel-free worktree.

The current continuation adds `requirements-notebook-smoke.txt` with pinned
runtime versions consumed by the opt-in workflow, plus focused remediation for
LLM structured output and agent state recovery:

- `llm/interview-prep/structured-output-remediation.md`
- `agentic-ai/interview-prep/state-recovery-remediation.md`

These pages are linked from Q20/Q4 and their section READMEs, and were
delivered in `4435321`.

## Recommended next work

1. Keep the notebook workflow manually triggered for heavyweight execution and
   extend its representative matrix only when dependencies remain deterministic.

## Blockers

None.
